import styled from 'styled-components';

const Wrapper = styled.div`
     max-width:960px;
     padding-left: 10px;
     padding-right: 10px;
     margin-left:auto;
     margin-right:auto;
`;

export default Wrapper;